% Hechos definidos para el BNF

% Lugares

lugar(["cartago"|S], S).
lugar(["san jose"|S], S).
lugar(["heredia"|S], S).
lugar(["puntarenas"|S], S).
lugar(["alajuela"|S], S).

%%%%% VERBOS %%%%%

% Verbo de composicion conjugados (queremos, gustaria, quier)
verbo_conjugado(["queremos"|S], S).
verbo_conjugado(["gustaria"|S], S).
verbo_conjugado(["quiero"|S], S).

